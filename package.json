{
  "name": "homework-helper",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "npx eslint . && npx prettier --check .",
    "test": "echo \"Error: no test specified\"",
    "dev": "nodemon main.js",
    "start": "node main.js",
    "format": "npx prettier --write . && npx eslint ."
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "create-react-app": "^5.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "prettier": "3.0.3"
  }
}
